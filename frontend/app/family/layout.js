export const metadata = {
  icons: {
    icon: "/family-icon.svg",
    shortcut: "/family-icon.svg",
    apple: "/family-icon.svg",
  },
};

export default function FamilyLayout({ children }) {
  return children;
}
