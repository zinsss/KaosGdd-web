import { NextResponse } from "next/server";
export async function GET(request) { const base = process.env.NEXT_PUBLIC_API_BASE || "http://127.0.0.1:8000"; const { searchParams } = new URL(request.url); const query = searchParams.toString(); const res = await fetch(base + "/files" + (query ? `?${query}` : ""), { cache: "no-store" }); const data = await res.json(); return NextResponse.json(data, { status: res.status }); }
export async function POST(request) { const base = process.env.NEXT_PUBLIC_API_BASE || "http://127.0.0.1:8000"; const bytes = await request.arrayBuffer(); const res = await fetch(base + "/files", { method: "POST", body: bytes, headers: { "content-type": "application/octet-stream", "x-file-name": request.headers.get("x-file-name") || "uploaded-file", "x-file-type": request.headers.get("x-file-type") || "application/octet-stream" }, cache: "no-store" }); const data = await res.json(); return NextResponse.json(data, { status: res.status }); }
