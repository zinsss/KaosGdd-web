"use client";
import LinkedItemsBlock from "./LinkedItemsBlock";
function formatBytes(value) { const size = Number(value || 0); if (!Number.isFinite(size) || size < 1024) return `${size} B`; if (size < 1024 * 1024) return `${Math.round(size / 1024)} KB`; return `${(size / (1024 * 1024)).toFixed(1)} MB`; }
export default function FileDetailPanel({ item }) { return <main className="page"><div className="detailBackLinkRow"><a className="taskLink backLink" href="/files">&lt; Back to Files</a></div><section className="panel"><div className="detailPageLabel">• File Detail</div><div className="sectionTitle detailMainTitle">{item.original_filename || item.title}</div><div className="detailReadBlock"><div className="detailReadRow"><div className="detailReadLabel">Type</div><div className="detailReadContent withDivider">{item.mime_type || "application/octet-stream"}</div></div><div className="detailReadRow"><div className="detailReadLabel">Size</div><div className="detailReadContent withDivider">{formatBytes(item.size_bytes)}</div></div><div className="detailReadRow"><div className="detailReadLabel">Added</div><div className="detailReadContent withDivider">{item.created_at_display || item.created_at}</div></div>{item.updated_at_display ? <div className="detailReadRow"><div className="detailReadLabel">Updated</div><div className="detailReadContent withDivider">{item.updated_at_display}</div></div> : null}<div className="detailReadRow"><div className="detailReadLabel">Open</div><div className="detailReadContent withDivider"><a className="taskLink" href={`/api/files/${item.id}/open`} target="_blank" rel="noreferrer">Open / Download</a></div></div><LinkedItemsBlock links={item.links} /></div></section></main>; }
